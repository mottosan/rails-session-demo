<%= form_with(model: sample_session, local: true) do |form| %>
  <% if sample_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sample_session.errors.count, "error") %> prohibited this sample_session from being saved:</h2>

      <ul>
        <% sample_session.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <div class="form-group">
      <%= form.label :key %>
      <%= form.text_field :key, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= form.label :value %>
      <%= form.text_field :value, class: 'form-control' %>
    </div>
    <%= form.submit %>
  </div>
<% end %>
